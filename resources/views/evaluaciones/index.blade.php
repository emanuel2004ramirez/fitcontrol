@extends('layouts.app') @section('title','Evaluaciones') @section('content')<x-page-header title="Evaluaciones físicas"><x-button :href="route('evaluaciones.create')">Nueva evaluación</x-button></x-page-header><x-card title="Buscar" class="mb-4"><form class="d-flex gap-2"><input class="form-control" name="texto" value="{{ $filtros['texto']??'' }}" placeholder="Cliente o método"><x-button type="submit">Buscar</x-button></form></x-card><x-card><table class="table"><thead><tr><th>Fecha</th><th>Cliente</th><th>Evaluador</th><th>Método</th><th>Medidas</th><th></th></tr></thead><tbody>@forelse($evaluaciones as $e)<tr><td>{{ \Illuminate\Support\Carbon::parse($e->evaluada_at)->format('d/m/Y H:i') }}</td><td>{{ $e->cliente }}</td><td>{{ $e->evaluador }}</td><td>{{ $e->metodo?:'—' }}</td><td>{{ $e->medidas }}</td><td><a href="{{ route('evaluaciones.show',$e->id) }}" class="btn btn-sm btn-outline-primary"><i class="bi bi-eye"></i></a></td></tr>@empty<tr><td colspan="6">Sin evaluaciones.</td></tr>@endforelse</tbody></table>{{ $evaluaciones->links() }}</x-card>@endsection
